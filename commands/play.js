const ytdl = require("ytdl-core");
const ytsearch = require("yt-search");

module.exports = {
    name: "play",
    description: "play music with provided link or searches for one",
    execute(message, args) {
        
    }
}